#!/bin/bash
# PostgreSQL 12 environment setup
# Generated by pgctl use 12

# Odebrání všech PostgreSQL binárek z PATH
export PATH=$(echo "$PATH" | sed 's|:/usr/lib/postgresql/[^/]*/bin||g')

# Přidání PostgreSQL 12 na začátek PATH
export PATH="/usr/lib/postgresql/12/bin:$PATH"

echo "🐘 PostgreSQL 12 aktivní (psql $(psql --version | head -1))"
