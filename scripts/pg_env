#!/bin/bash
# PostgreSQL 9.5 environment setup
# Generated by pgctl use 9.5

# Odebrání všech PostgreSQL binárek z PATH
export PATH=$(echo "$PATH" | sed 's|:/usr/lib/postgresql/[^/]*/bin||g')

# Přidání PostgreSQL 9.5 na začátek PATH
export PATH="/usr/lib/postgresql/9.5/bin:$PATH"

echo "🐘 PostgreSQL 9.5 aktivní (psql $(psql --version | head -1))"
