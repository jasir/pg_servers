#!/bin/bash

# PostgreSQL Control Tool (pgctl)
# Správa více verzí PostgreSQL přes Docker

PG_SERVERS_ROOT="$HOME/pg_servers"
ENV_FILE="$PG_SERVERS_ROOT/.env"
COMPOSE_FILE="$PG_SERVERS_ROOT/docker/docker-compose.yml"

# Načtení environment proměnných
if [ -f "$ENV_FILE" ]; then
    source "$ENV_FILE"
fi

# Funkce pro instalaci PostgreSQL klientů
install_clients() {
    echo "📦 Instaluji PostgreSQL klienty pro všechny podporované verze..."

    # Seznam verzí k instalaci
    local versions=("16" "15" "14" "13" "12")

    # Kontrola, zda jsme na Ubuntu/Debian systému
    if ! command -v apt-get >/dev/null 2>&1; then
        echo "❌ Tento skript vyžaduje apt-get (Ubuntu/Debian)"
        return 1
    fi

    # Přidání PostgreSQL APT repozitáře pro starší verze
    echo "🔄 Přidávám PostgreSQL APT repozitář..."
    if ! grep -q "apt.postgresql.org" /etc/apt/sources.list /etc/apt/sources.list.d/* 2>/dev/null; then
        echo "📦 Přidávám PostgreSQL repozitář..."
        sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'

        # Přidání klíče
        curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/postgresql.gpg

        echo "🔄 Aktualizuji seznam balíčků..."
        sudo apt-get update
    fi

    # Instalace postgresql-client-common
    echo "📦 Instaluji postgresql-client-common..."
    sudo apt-get install -y postgresql-client-common

    # Instalace klientů pro každou verzi
    local installed=0
    for version in "${versions[@]}"; do
        echo "📦 Instaluji postgresql-client-$version..."
        if sudo apt-get install -y "postgresql-client-$version"; then
            echo "✅ PostgreSQL client $version nainstalován"
            installed=$((installed + 1))
        else
            echo "⚠️  Nepodařilo se nainstalovat postgresql-client-$version"
        fi
    done

    echo ""
    if [ $installed -gt 0 ]; then
        echo "✅ Instalace dokončena ($installed verzí)"
        echo "📋 Nainstalované verze:"
        list_installed_versions
    else
        echo "❌ Nepodařilo se nainstalovat žádné PostgreSQL klienty"
        return 1
    fi
}

# Funkce pro zobrazení nainstalovaných verzí
list_installed_versions() {
    echo "🔍 Nainstalované PostgreSQL klienty:"
    echo ""

    local found=false
    for dir in /usr/lib/postgresql/*; do
        if [ -d "$dir/bin" ] && [ -f "$dir/bin/psql" ]; then
            local version=$(basename "$dir")
            local psql_path="$dir/bin/psql"
            local psql_version=$($psql_path --version | head -1)
            echo "  • PostgreSQL $version: $psql_version"
            found=true
        fi
    done

    if [ "$found" = false ]; then
        echo "  Žádné PostgreSQL klienty nenalezeny"
        echo "  Spusťte: pgctl install"
    fi
}

# Funkce pro nastavení PATH pro specifickou verzi
set_pg_version() {
    local version="$1"

    if [ -z "$version" ]; then
        echo "❌ Chybí verze. Použití: pgctl use <verze>"
        echo "Dostupné verze: $(list_available_versions)"
        return 1
    fi

    local pg_path="/usr/lib/postgresql/$version/bin"

    if [ ! -d "$pg_path" ]; then
        echo "❌ PostgreSQL $version není nainstalován"
        echo "Spusťte: pgctl install"
        return 1
    fi

    # Odebrání všech PostgreSQL binárek z PATH
    export PATH=$(echo "$PATH" | sed 's|:/usr/lib/postgresql/[^/]*/bin||g')

    # Přidání požadované verze na začátek PATH
    export PATH="$pg_path:$PATH"

    echo "✅ Nastavena PostgreSQL verze $version"
    echo "📍 PATH: $pg_path"
    echo "🔍 Verze psql: $(psql --version | head -1)"
}

# Funkce pro získání seznamu dostupných verzí
list_available_versions() {
    local versions=""
    for dir in /usr/lib/postgresql/*; do
        if [ -d "$dir/bin" ]; then
            local version=$(basename "$dir")
            versions="$versions $version"
        fi
    done
    echo "$versions" | sed 's/^ *//'
}

# Funkce pro zobrazení aktuální verze
show_current_version() {
    local psql_path=$(which psql 2>/dev/null)
    if [ -n "$psql_path" ]; then
        local version=$(psql --version | head -1)
        echo "🔍 Aktuální PostgreSQL klient: $version"
        echo "📍 Cesta: $psql_path"
    else
        echo "❌ PostgreSQL klient není v PATH"
        echo "Spusťte: pgctl use <verze> nebo pgctl install"
    fi
}
show_help() {
    echo "PostgreSQL Control Tool (pgctl)"
    echo ""
    echo "Použití:"
    echo "  pgctl start <verze>     - Spustí PostgreSQL kontejner"
    echo "  pgctl stop <verze>      - Zastaví PostgreSQL kontejner"
    echo "  pgctl restart <verze>   - Restartuje PostgreSQL kontejner"
    echo ""
    echo "  pgctl status            - Zobrazí stav všech kontejnerů"
    echo "  pgctl list              - Seznam dostupných verzí"
    echo ""
    echo "  pgctl logs <verze>      - Zobrazí logy kontejneru"
    echo "  pgctl up                - Kompletní nastavení (build + start)"
    echo ""
    echo "  pgctl install           - Nainstaluje PostgreSQL klienty"
    echo "  pgctl use <verze>       - Nastaví PostgreSQL klienta"
    echo "  pgctl which             - Zobrazí aktuální verzi klienta"
    echo "  pgctl clients           - Seznam nainstalovaných klientů"
    echo ""
    echo "Dostupné verze: 9.5, 9.6, 12, 15, 16"
    echo ""
    echo "Příklady:"
    echo "  pgctl start 9.5         # Spustí PostgreSQL 9.5 na portu 5432"
    echo "  pgctl install           # Nainstaluje všechny PostgreSQL klienty"
    echo "  pgctl use 15            # Nastaví PostgreSQL 15 klienta"
    echo "  pgctl which             # Zobrazí aktuální verzi"
    echo ""
    echo "  pgctl status            # Zobrazí běžící kontejnery"
    echo "  pgctl up                # První spuštění a build custom image"
}

# Funkce pro normalizaci verze
normalize_version() {
    case "$1" in
        9.5|95) echo "9.5" ;;
        9.6|96) echo "9.6" ;;
        12|12.*) echo "12" ;;
        15|15.*) echo "15" ;;
        16|16.*) echo "16" ;;
        *) echo "unknown" ;;
    esac
}

# Funkce pro získání názvu kontejneru
get_container_name() {
    local version=$(normalize_version "$1")
    case "$version" in
        9.5) echo "pg95" ;;
        9.6) echo "pg96" ;;
        12) echo "pg12" ;;
        15) echo "pg15" ;;
        16) echo "pg16" ;;
        *) echo "" ;;
    esac
}

# Funkce pro získání portu
get_port() {
    local version=$(normalize_version "$1")
    case "$version" in
        9.5) echo "5432" ;;
        9.6) echo "5433" ;;
        12) echo "5434" ;;
        15) echo "5435" ;;
        16) echo "5437" ;;
        *) echo "" ;;
    esac
}

# Funkce pro start kontejneru
start_container() {
    local version=$(normalize_version "$1")
    local container=$(get_container_name "$version")
    local port=$(get_port "$version")

    if [ "$container" = "" ]; then
        echo "❌ Neznámá verze: $1"
        echo "Dostupné verze: 9.5, 9.6, 12, 15, 16"
        return 1
    fi

    echo "🚀 Spouštím PostgreSQL $version na portu $port..."

    if ! docker compose -f "$COMPOSE_FILE" up -d "postgres${version//./}"; then
        echo "❌ Chyba při spuštění kontejneru $container"
        return 1
    fi

    echo "✅ PostgreSQL $version spuštěn"
    echo "📡 Připojení: psql -h localhost -p $port -U postgres"
}

# Funkce pro stop kontejneru
stop_container() {
    local version=$(normalize_version "$1")
    local container=$(get_container_name "$version")

    if [ "$container" = "" ]; then
        echo "❌ Neznámá verze: $1"
        return 1
    fi

    echo "🛑 Zastavuji PostgreSQL $version..."

    if docker compose -f "$COMPOSE_FILE" stop "postgres${version//./}"; then
        echo "✅ PostgreSQL $version zastaven"
    else
        echo "❌ Chyba při zastavování kontejneru $container"
        return 1
    fi
}

# Funkce pro restart kontejneru
restart_container() {
    local version=$(normalize_version "$1")
    echo "🔄 Restartuji PostgreSQL $version..."
    stop_container "$version" && start_container "$version"
}




# Funkce pro zobrazení statusu
show_status() {
    echo "📊 Status PostgreSQL kontejnerů:"
    echo ""

    for version in "9.5" "9.6" "12" "15" "16"; do
        local container=$(get_container_name "$version")
        local port=$(get_port "$version")
        local status=""

        if docker ps --format "table {{.Names}}\t{{.Status}}" | grep -q "^$container"; then
            status="🟢 Běží"
        elif docker ps -a --format "table {{.Names}}\t{{.Status}}" | grep -q "^$container"; then
            status="🔴 Zastaven"
        else
            status="⚪ Nevytvořen"
        fi

        printf "  PostgreSQL %-3s (port %s): %s\n" "$version" "$port" "$status"
    done
}

# Funkce pro seznam verzí
list_versions() {
    echo "📋 Dostupné verze PostgreSQL:"
    echo ""
    echo "  • 9.5  (port 5432)"
    echo "  • 9.6  (port 5433)"
    echo "  • 12   (port 5434)"
    echo "  • 15   (port 5435)"
    echo "  • 16   (port 5437)"
    echo ""
    echo "Použití: pgctl start <verze>"
}


# Funkce pro zobrazení logů
show_logs() {
    local version=$(normalize_version "$1")
    local container=$(get_container_name "$version")

    if [ "$container" = "" ]; then
        echo "❌ Neznámá verze: $1"
        return 1
    fi

    echo "📜 Logy pro PostgreSQL $version:"
    docker compose -f "$COMPOSE_FILE" logs -f "postgres${version//./}"
}

# Hlavní část skriptu
case "$1" in
    start)
        if [ -z "$2" ]; then
            echo "❌ Chybí verze. Použití: pgctl start <verze>"
            exit 1
        fi
        start_container "$2"
        ;;
    stop)
        if [ -z "$2" ]; then
            echo "❌ Chybí verze. Použití: pgctl stop <verze>"
            exit 1
        fi
        stop_container "$2"
        ;;
    restart)
        if [ -z "$2" ]; then
            echo "❌ Chybí verze. Použití: pgctl restart <verze>"
            exit 1
        fi
        restart_container "$2"
        ;;
    status)
        show_status
        ;;
    list)
        list_versions
        ;;
    logs)
        if [ -z "$2" ]; then
            echo "❌ Chybí verze. Použití: pgctl logs <verze>"
            exit 1
        fi
        show_logs "$2"
        ;;
    up)
        exec "$PG_SERVERS_ROOT/scripts/pgctl_up"
        ;;
    install)
        install_clients
        ;;
    use)
        set_pg_version "$2"
        ;;
    which)
        show_current_version
        ;;
    clients)
        list_installed_versions
        ;;
    help|--help|-h)
        show_help
        ;;
    "")
        show_help
        ;;
    *)
        echo "❌ Neznámý příkaz: $1"
        echo ""
        show_help
        exit 1
        ;;
esac